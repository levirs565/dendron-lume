{%- set attrs -%}
  role="{{"group" if is_child else "tree"}}"
  {% if label %}
    aria-label="{{label}}"
  {% endif %}
{%- endset -%}
{% comp "tree_list", is_child=is_child, attrs=attrs %}
  {%- for item in items -%}
    {{ comp.nav_item({item: item}) | safe }} 
  {%- endfor -%}
{% endcomp %}