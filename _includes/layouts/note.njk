<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.2/dist/katex.css" />
    <link rel="stylesheet" href="/styles.css" />
    <link rel="stylesheet" href="/components.css" />
    <link rel="canonical" href="{{ url | canonicalUrl }}" />
    <meta name="article:published_time" content="{{created | date('DATE')}}" />
    <meta name="article:modified_time " content="{{updated | date('DATE')}}" />
    <script>
      window.noteId = "{{id}}";
    </script>
    <script src="/main.js"></script>
  </head>
  <body
    class="data-[modal=true]:overflow-hidden data-[modal-name=nav]:lg:overflow-scroll data-[modal-name=toc]:xl:overflow-scroll"
  >
    {% include "templates/appbar.njk" %} {% include "templates/navbar.njk" %}
    <main class="mt-12 lg:ml-78 xl:mr-78 py-4 px-8">
      {% include "templates/breadcrumb.njk" %}
      <article class="prose md:max-w-none prose-headings:scroll-mt-16 mx-auto">
        <header>
          <h1 class="mb-2">{{ title }}</h1>
          <p class="my-0 text-sm text-slate-700">
            Created at <time>{{created | date('DATE')}}</time>. 
            Updated at <time>{{updated | date('DATE')}}</time>.
          </p>
        </header>
        {{ content | safe }}
        <footer>
          <hr />
          {% if subnotes | length > 0 %}
          <strong>Halaman Anak</strong>
          {{ comp.related_links({ list: subnotes }) | safe }}
          {% endif %} {% if backlinks | length > 0 %}
          <strong>Halaman yang Merujuk Halaman Ini</strong>
          {{ comp.related_links({ list: backlinks }) | safe }}
          {% endif %}
        </footer>
      </article>
    </main>
    {% include "templates/toc.njk" %}
  </body>
</html>
